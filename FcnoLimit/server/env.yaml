# env.yaml - Solo valores no sensibles
# Las credenciales de DB se configuran directamente en Render Dashboard

DB_PORT: 5432
DB_SSL: "true"
PORT: 3001
NODE_ENV: "production"

# CORS se configura en Render Dashboard para mayor flexibilidad
# JWT_SECRET se configura en Render Dashboard por seguridad
# DB_* se configuran en Render Dashboard por seguridad
